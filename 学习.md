# 黑马点评项目面试导向学习指南

> 目标：围绕你简历中写到的项目亮点，有针对性地学习源码、业务流程和面试回答，确保能讲清楚“为什么这样做、怎么实现、解决了什么问题、还有什么不足”。

## 一、你的简历内容对应的重点模块

你的简历写法：

> 基于 Spring Boot + Redis 构建的本地生活点评系统，提供商户查询、探店笔记、点赞、关注、签到等功能，重点针对高并发场景进行缓存优化与系统性能提升。

项目亮点：

1. 使用 Redis 缓存热点商户数据，降低数据库访问压力，提升接口响应速度
2. 使用 Redis SortedSet 实现用户关注动态 Feed 流，支持滚动分页加载
3. 使用 Lua 脚本保证秒杀资格校验、库存扣减等操作的原子性，避免超卖与重复下单
4. 使用 Redis GEO 实现附近商户查询，满足本地生活业务中的地理位置检索需求

因此学习重点按优先级排序如下：

```text
第一优先级：商户缓存、秒杀 Lua、Feed 流、GEO 附近商户
第二优先级：登录鉴权、点赞、关注、签到 Bitmap
第三优先级：普通 CRUD、文件上传、评论模块、前端页面
```

面试时最容易被追问的是：

```text
1. 商户缓存如何解决缓存穿透、缓存击穿？
2. 秒杀为什么要用 Lua？Lua 具体保证了什么原子性？
3. 一人一单怎么保证？Redis 校验之后为什么还要数据库校验？
4. Redis Stream 在秒杀中起什么作用？
5. Feed 流为什么用 SortedSet？滚动分页怎么解决重复数据？
6. Redis GEO 查询附近商户的流程是什么？
```

---

## 二、模块 1：热点商户缓存

### 1. 对应源码

重点看这些文件：

```text
hm-dianping/src/main/java/com/hmdp/controller/ShopController.java
hm-dianping/src/main/java/com/hmdp/service/impl/ShopServiceImpl.java
hm-dianping/src/main/java/com/hmdp/utils/CacheClient.java
hm-dianping/src/main/java/com/hmdp/utils/RedisConstants.java
```

核心方法：

```text
ShopServiceImpl.queryById()
CacheClient.queryWithPassThrough()
CacheClient.queryWithLogicalExpire()
CacheClient.setWithLogicalExpire()
ShopServiceImpl.update()
```

### 2. 你需要掌握的业务流程

查询商户详情的大致流程：

```text
前端请求商户详情
        ↓
ShopController 接收请求
        ↓
ShopServiceImpl.queryById(id)
        ↓
先查 Redis 缓存
        ↓
命中：直接返回
        ↓
未命中：根据不同策略决定是否查 MySQL
        ↓
查询 MySQL 后写入 Redis
        ↓
返回商户数据
```

当前项目中 `queryById()` 实际使用的是：

```java
cacheClient.queryWithLogicalExpire(...)
```

也就是使用“逻辑过期”处理热点数据缓存击穿。

### 3. 缓存穿透

缓存穿透指：

```text
用户请求一个数据库中根本不存在的数据，Redis 查不到，MySQL 也查不到。
如果大量这种请求进来，会绕过 Redis 直接打到数据库。
```

项目中的解决方案：

```text
缓存空值。
```

流程：

```text
Redis 未命中
        ↓
查 MySQL
        ↓
MySQL 也不存在
        ↓
向 Redis 写入空字符串，设置较短 TTL
        ↓
后续请求命中空值，直接返回，不再访问数据库
```

源码位置：

```text
CacheClient.queryWithPassThrough()
```

注意：当前 `ShopServiceImpl.queryById()` 里缓存穿透方案被注释掉了，但 `CacheClient` 中保留了完整实现。面试时可以说“项目中封装过缓存空值方案，用于解决缓存穿透；热点商户查询最终采用逻辑过期策略”。

### 4. 缓存击穿

缓存击穿指：

```text
某个热点 key 过期的一瞬间，大量请求同时访问这个热点数据，Redis 查不到，所有请求都打到 MySQL。
```

项目中主要使用：

```text
逻辑过期 + 互斥锁 + 异步重建缓存
```

流程：

```text
查询 Redis
        ↓
Redis 中有数据，但数据中带有 expireTime
        ↓
如果逻辑时间未过期：直接返回
        ↓
如果逻辑时间已过期：尝试获取互斥锁
        ↓
获取锁成功：开启后台线程查询 MySQL 并重建缓存
        ↓
获取锁失败：直接返回旧数据
```

这种方案的核心取舍：

```text
优点：热点 key 过期时不会让大量请求打到数据库，接口响应也很快。
缺点：在缓存重建完成前，用户可能短暂读到旧数据。
```

### 5. 数据一致性

商户更新时：

```text
先更新 MySQL
再删除 Redis 缓存
```

源码位置：

```text
ShopServiceImpl.update()
```

为什么不是“更新数据库后更新缓存”？

```text
因为更新缓存容易出现并发写入顺序问题，导致旧数据覆盖新缓存。
相比之下，更新数据库后删除缓存更简单、更常见。
```

面试回答可以这样说：

> 我们采用 Cache Aside 模式。读请求先查 Redis，未命中再查数据库并回写缓存；写请求先更新数据库，再删除缓存。这样可以降低缓存和数据库不一致的概率。对于热点商户，还通过逻辑过期加互斥锁异步重建缓存，避免热点 key 失效瞬间大量请求打到数据库。

### 6. 面试高频问题

#### Q1：你们怎么解决缓存穿透？

答：

> 对不存在的数据缓存空值，并设置较短过期时间。这样同一个非法 id 后续请求会直接命中 Redis 空值，不再访问数据库，降低恶意请求或异常请求对数据库的压力。

#### Q2：你们怎么解决缓存击穿？

答：

> 对热点商户使用逻辑过期。Redis 中保存的不只是商户数据，还包含逻辑过期时间。请求发现数据过期后，并不会立即阻塞等待数据库，而是尝试获取互斥锁。拿到锁的线程异步重建缓存，其他线程直接返回旧数据。这样牺牲短时间一致性，换取高并发场景下的可用性和低延迟。

#### Q3：逻辑过期和普通 TTL 有什么区别？

答：

> 普通 TTL 到期后 key 会直接失效，大量请求可能同时打到数据库。逻辑过期是 key 不真正删除，而是在 value 中存一个过期时间。即使逻辑上过期了，也能先返回旧数据，再异步重建缓存，所以更适合热点数据。

#### Q4：这个方案有什么缺点？

答：

> 逻辑过期可能返回短时间旧数据，所以更适合商户详情这类读多写少、允许短暂不一致的场景。如果是库存、余额这类强一致数据，就不能简单使用这种方案。

---

## 三、模块 2：秒杀优惠券与 Lua 原子校验

### 1. 对应源码

重点看这些文件：

```text
hm-dianping/src/main/java/com/hmdp/controller/VoucherOrderController.java
hm-dianping/src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java
hm-dianping/src/main/java/com/hmdp/service/impl/VoucherServiceImpl.java
hm-dianping/src/main/resources/seckill.lua
hm-dianping/src/main/java/com/hmdp/utils/RedisIdWorker.java
hm-dianping/src/main/java/com/hmdp/config/RedissonConfig.java
```

核心方法：

```text
VoucherServiceImpl.addSeckillVoucher()
VoucherOrderServiceImpl.seckillVoucher()
VoucherOrderServiceImpl.handleVoucherOrder()
VoucherOrderServiceImpl.createVoucherOrder()
seckill.lua
```

### 2. 秒杀整体流程

```text
添加秒杀券
        ↓
MySQL 保存优惠券信息
        ↓
Redis 预热库存 seckill:stock:{voucherId}

用户发起秒杀请求
        ↓
生成全局唯一订单 id
        ↓
执行 Lua 脚本
        ↓
Lua 在 Redis 中原子判断库存、判断是否重复下单、扣减 Redis 库存、记录用户、写入 Stream 队列
        ↓
接口快速返回订单 id
        ↓
后台线程消费 Redis Stream
        ↓
Redisson 按 userId 加分布式锁
        ↓
再次检查 MySQL 是否已下单
        ↓
扣减 MySQL 库存并保存订单
        ↓
ACK 消息
```

### 3. Lua 脚本做了什么

源码：

```text
hm-dianping/src/main/resources/seckill.lua
```

Lua 脚本核心逻辑：

```text
1. 获取 voucherId、userId、orderId
2. 拼接库存 key：seckill:stock:{voucherId}
3. 拼接下单用户 set key：seckill:order:{voucherId}
4. 判断 Redis 库存是否大于 0
5. 判断用户是否已经在下单 Set 中
6. Redis 库存 -1
7. 将 userId 加入 Set，表示该用户已抢购
8. 将订单消息写入 stream.orders
9. 返回 0 表示成功
```

返回值含义：

```text
0：有资格下单
1：库存不足
2：重复下单
```

### 4. 为什么要用 Lua

Redis 单条命令是原子的，但秒杀资格判断包含多个操作：

```text
查库存
判断库存
判断用户是否下单
扣减库存
记录用户已下单
写入消息队列
```

如果这些命令分多次发送，在高并发下可能出现竞态条件。

Lua 的作用：

```text
把多个 Redis 操作打包成一个脚本，让 Redis 以单线程方式一次性执行完，保证这些 Redis 操作之间不会被其他请求插入。
```

面试回答要注意：

> Lua 保证的是 Redis 内部“资格校验 + Redis 库存扣减 + 记录已下单用户 + 写入 Stream”的原子性。真正的 MySQL 库存扣减是在后台异步消费时完成的，并通过 `stock > 0` 条件更新兜底防止超卖。

这个说法比“Lua 保证数据库库存扣减原子性”更准确。

### 5. 为什么还需要 MySQL 再校验一人一单

虽然 Lua 已经在 Redis 里判断过用户是否重复下单，但后台落库时仍然做了：

```text
根据 user_id + voucher_id 查询订单数量
如果 count > 0，说明已经买过，直接返回
```

原因：

```text
Redis 层是前置快速校验，用来抗高并发。
MySQL 层是最终数据落库兜底，防止极端情况下数据不一致。
```

更稳妥的生产级方案还可以加：

```text
数据库唯一索引 user_id + voucher_id
```

这样可以从数据库约束层面保证一人一单。

### 6. 为什么使用 Redis Stream

如果秒杀请求直接操作数据库：

```text
每个请求都要查订单、扣库存、插订单，数据库压力很大，接口响应慢。
```

使用 Redis Stream 后：

```text
请求线程只负责快速完成资格判断并写入消息队列
后台线程异步消费订单消息并落库
```

优点：

```text
1. 削峰填谷，降低数据库瞬时压力
2. 接口响应更快
3. Stream 支持消费者组、ACK、Pending List，消息可靠性比普通 List 更好
```

项目中的 Stream：

```text
stream.orders
消费者组：g1
消费者：c1
```

注意：需要提前创建消费者组，否则可能出现：

```text
NOGROUP No such key 'stream.orders' or consumer group 'g1'
```

创建命令：

```text
XGROUP CREATE stream.orders g1 $ MKSTREAM
```

### 7. Redisson 分布式锁的作用

源码位置：

```text
VoucherOrderServiceImpl.handleVoucherOrder()
```

锁 key：

```text
lock:order:{userId}
```

作用：

```text
保证同一个用户的多个下单请求在分布式部署下串行执行，避免重复创建订单。
```

为什么不用 synchronized？

```text
synchronized 只在单个 JVM 内有效。
如果项目部署多个实例，同一个用户的请求可能打到不同机器，synchronized 无法跨进程生效。
Redis/Redisson 分布式锁可以让多个服务实例共享同一把锁。
```

### 8. 面试高频问题

#### Q1：秒杀为什么不能直接操作数据库？

答：

> 秒杀流量瞬时很高，如果每个请求都直接访问数据库，会造成大量行锁竞争和数据库压力。项目中把库存和用户资格校验前置到 Redis，通过 Lua 原子执行，校验成功后写入 Redis Stream，后台异步落库，从而实现削峰和快速响应。

#### Q2：Lua 脚本具体解决了什么问题？

答：

> 它解决的是多个 Redis 操作之间的原子性问题。比如判断库存、判断是否重复下单、扣减库存、记录用户已下单，如果分开执行，高并发下可能被其他请求插入导致超卖或重复下单。Lua 脚本在 Redis 中一次执行完这些逻辑，保证不会被打断。

#### Q3：你们怎么防止超卖？

答：

> 第一层是在 Redis Lua 中判断库存大于 0 才扣减 Redis 库存；第二层是在 MySQL 落库时使用 `stock = stock - 1` 并加上 `stock > 0` 条件，保证数据库扣减不会扣成负数。

#### Q4：你们怎么防止重复下单？

答：

> 第一层是在 Lua 中用 Redis Set 记录已下单用户，重复用户直接返回失败；第二层是在异步落库时按 userId 和 voucherId 查询订单，防止重复保存；同时按 userId 使用 Redisson 分布式锁，让同一个用户的订单处理串行化。生产环境还可以加数据库唯一索引兜底。

#### Q5：Redis Stream 消息消费失败怎么办？

答：

> 项目中正常消费成功后会 ACK。如果消费异常，会读取 Pending List 中未确认的消息继续处理，避免消息丢失。这比普通 List 更适合订单这种需要可靠处理的场景。

---

## 四、模块 3：关注动态 Feed 流

### 1. 对应源码

重点看这些文件：

```text
hm-dianping/src/main/java/com/hmdp/controller/BlogController.java
hm-dianping/src/main/java/com/hmdp/service/impl/BlogServiceImpl.java
hm-dianping/src/main/java/com/hmdp/service/impl/FollowServiceImpl.java
hm-dianping/src/main/java/com/hmdp/dto/ScrollResult.java
hm-dianping/src/main/java/com/hmdp/utils/RedisConstants.java
```

核心方法：

```text
BlogServiceImpl.saveBlog()
BlogServiceImpl.queryBlogOfFollow()
FollowServiceImpl.follow()
FollowServiceImpl.isFollow()
FollowServiceImpl.followCommons()
```

### 2. Feed 流业务流程

用户发布探店笔记：

```text
用户发布 Blog
        ↓
保存 Blog 到 MySQL
        ↓
查询该用户的所有粉丝
        ↓
把 Blog ID 推送到每个粉丝的收件箱
        ↓
收件箱使用 Redis ZSet 存储
```

粉丝查看关注动态：

```text
用户打开关注动态页
        ↓
从自己的 feed:{userId} ZSet 中按时间倒序查询 Blog ID
        ↓
根据 Blog ID 批量查 MySQL
        ↓
按 Redis 返回的 ID 顺序排序
        ↓
返回博客列表、minTime、offset
```

### 3. 为什么用 SortedSet

Feed 流需要：

```text
1. 按时间排序
2. 支持范围查询
3. 支持滚动分页
```

Redis SortedSet 适合这个场景：

```text
member：blogId
score：发布时间戳
key：feed:{粉丝用户id}
```

示例：

```text
feed:101
  blogId=15, score=1710000000000
  blogId=16, score=1710000001000
```

### 4. 推模式、拉模式、推拉结合

项目中使用的是：

```text
推模式
```

也就是作者一发布博客，就把博客 ID 推送到所有粉丝的收件箱。

优点：

```text
粉丝查询时速度快，直接读自己的收件箱。
适合普通用户、粉丝数量不特别大的场景。
```

缺点：

```text
如果作者粉丝非常多，发布时需要写入大量粉丝收件箱，写扩散严重。
```

面试可以补充：

> 如果是大 V 用户，可以使用拉模式或推拉结合。普通用户使用推模式，大 V 只保存到发件箱，粉丝刷 Feed 时再拉取合并。

### 5. 滚动分页为什么不用普通 page/size

如果用普通分页：

```text
第一页：查 0~9
第二页：查 10~19
```

当中间有新数据插入时，数据位置会变化，可能导致：

```text
重复数据或漏数据
```

项目使用：

```text
基于 score 的滚动分页
```

核心参数：

```text
max：上一次查询返回的最小时间戳
offset：当多个数据 score 相同时，跳过的数量
```

查询命令思想：

```text
ZREVRANGEBYSCORE feed:{userId} max 0 LIMIT offset count
```

返回：

```text
list：当前页博客列表
minTime：当前页最小时间戳
offset：相同 minTime 的偏移量
```

### 6. 面试高频问题

#### Q1：Feed 流为什么用 SortedSet？

答：

> 因为关注动态需要按发布时间倒序展示，并且要支持滚动分页。SortedSet 的 score 可以保存时间戳，member 保存 blogId，通过按 score 范围倒序查询，可以很方便地实现按时间排序的 Feed 流。

#### Q2：Feed 流是推模式还是拉模式？

答：

> 项目中使用推模式。用户发布博客后，系统查询他的粉丝列表，把博客 ID 写入每个粉丝的 Feed 收件箱 ZSet。这样粉丝查询时速度很快，但如果发布者粉丝特别多，会产生写扩散。生产中可以对大 V 使用拉模式或推拉结合。

#### Q3：滚动分页怎么避免重复？

答：

> 使用时间戳作为 score，每次查询返回当前页的最小时间戳 minTime。下一页查询时以 minTime 作为 max 继续查。如果有多条数据时间戳相同，就用 offset 记录已经读取过多少条相同 score 的数据，下次查询跳过这些数据，从而避免重复。

---

## 五、模块 4：Redis GEO 附近商户

### 1. 对应源码

重点看这些文件：

```text
hm-dianping/src/main/java/com/hmdp/controller/ShopController.java
hm-dianping/src/main/java/com/hmdp/service/impl/ShopServiceImpl.java
hm-dianping/src/test/java/com/hmdp/HmdpApplicationTest.java
hm-dianping/src/main/java/com/hmdp/utils/RedisConstants.java
```

核心方法：

```text
ShopServiceImpl.queryShopByType()
```

测试类中通常会有把商铺经纬度导入 Redis GEO 的逻辑。

### 2. GEO 数据结构设计

项目中按商铺类型分组存储 GEO：

```text
key：shop:geo:{typeId}
member：shopId
longitude/latitude：商户经纬度
```

这样查询某一类型附近商户时，只需要查对应类型的 GEO key。

### 3. 查询流程

```text
前端传入 typeId、current、x、y
        ↓
如果没有 x/y：走普通数据库分页
        ↓
如果有 x/y：使用 Redis GEO 按距离查询
        ↓
Redis 返回 shopId + distance
        ↓
手动分页截取需要的 shopId
        ↓
根据 shopId 批量查 MySQL
        ↓
使用 ORDER BY FIELD 保持 Redis 距离排序
        ↓
把 distance 填充到 Shop 对象
        ↓
返回给前端
```

### 4. 为什么 Redis GEO 结果还要查 MySQL

Redis GEO 中只存：

```text
shopId + 经纬度
```

完整商铺信息还在 MySQL 中，例如：

```text
商铺名称、图片、评分、地址、人均消费等
```

所以 GEO 只负责：

```text
地理位置筛选和距离排序
```

MySQL 负责：

```text
查询完整业务数据
```

### 5. 分页细节

Redis GEO 查询时使用：

```text
limit(end)
```

然后在 Java 中：

```text
skip(from)
```

原因：

```text
Redis GEO 本身不是传统关系型分页，不能直接 offset + limit。
所以先查到当前页结束位置，再在内存中跳过前面的数据。
```

这种方式适合数据量不是特别大的附近商户查询。如果数据量很大，需要进一步优化。

### 6. 面试高频问题

#### Q1：附近商户怎么实现？

答：

> 项目使用 Redis GEO。提前把商户的经纬度按商户类型写入 Redis，key 类似 `shop:geo:{typeId}`，member 是 shopId。查询时根据用户经纬度、商户类型和距离半径，通过 GEO 查询得到附近 shopId 和距离，再根据 shopId 批量查询 MySQL 获取完整商户信息。

#### Q2：为什么按 typeId 分 key？

答：

> 因为业务查询通常会指定商户类型，比如美食、酒店、休闲娱乐。按类型分 key 可以减少 GEO 查询范围，避免所有商户混在一个 GEO 集合里导致查询和过滤成本更高。

#### Q3：GEO 查询结果怎么保持距离顺序？

答：

> Redis 返回的是按距离排序的 shopId。后续 MySQL 使用 `ORDER BY FIELD(id, ...)` 按这些 id 的顺序排序，保证最终返回列表仍然是距离排序。

---

## 六、模块 5：点赞与关注

虽然你的简历亮点主要写 Feed 流，但 Feed 流依赖关注关系，探店笔记也涉及点赞，所以这两个模块也要掌握。

### 1. 点赞模块

源码：

```text
hm-dianping/src/main/java/com/hmdp/service/impl/BlogServiceImpl.java
```

核心方法：

```text
likeBlog()
queryBlogLikes()
isBlogLiked()
```

Redis 结构：

```text
key：blog:liked:{blogId}
类型：SortedSet
member：userId
score：点赞时间戳
```

为什么用 ZSet 而不是 Set？

```text
Set 只能判断是否存在，不能按点赞时间排序。
ZSet 既能判断用户是否点赞，又能按 score 查询最早点赞的 Top5 用户。
```

面试回答：

> 点赞使用 Redis ZSet，member 是用户 id，score 是点赞时间戳。判断是否点赞时查 score 是否存在；点赞时数据库 liked +1，同时写入 ZSet；取消点赞时数据库 liked -1，同时从 ZSet 移除。查询点赞 Top5 时按 ZSet 顺序取前 5 个用户。

### 2. 关注模块

源码：

```text
hm-dianping/src/main/java/com/hmdp/service/impl/FollowServiceImpl.java
```

Redis 结构：

```text
key：follows:{userId}
类型：Set
member：followUserId
```

Set 的作用：

```text
1. 判断是否关注
2. 查询共同关注时做 SINTER 交集
```

面试回答：

> 关注关系最终存储在 MySQL，同时在 Redis Set 中维护一份用户关注集合。判断是否关注可以用 SISMEMBER，共同关注可以对两个用户的关注集合做交集查询。

---

## 七、模块 6：登录鉴权与签到

这部分不是你简历亮点的核心，但属于项目基础能力，面试官经常从登录流程开始问。

### 1. 登录鉴权

源码：

```text
hm-dianping/src/main/java/com/hmdp/service/impl/UserServiceImpl.java
hm-dianping/src/main/java/com/hmdp/utils/RefreshTokenInterceptor.java
hm-dianping/src/main/java/com/hmdp/utils/LoginInterceptor.java
hm-dianping/src/main/java/com/hmdp/config/MvcConfig.java
hm-dianping/src/main/java/com/hmdp/utils/UserHolder.java
```

流程：

```text
用户请求验证码
        ↓
校验手机号格式
        ↓
生成 6 位验证码
        ↓
验证码存入 Redis，设置 TTL

用户登录
        ↓
校验手机号和验证码
        ↓
根据手机号查询用户，不存在则创建
        ↓
生成 token
        ↓
把 UserDTO 存入 Redis Hash
        ↓
返回 token 给前端

后续请求
        ↓
前端在 authorization 请求头携带 token
        ↓
RefreshTokenInterceptor 从 Redis 查用户
        ↓
查到后放入 UserHolder(ThreadLocal)
        ↓
刷新 token TTL
        ↓
LoginInterceptor 判断需要登录的接口是否有用户信息
```

面试回答：

> 登录后服务端生成 token，把用户简要信息存入 Redis Hash，并设置过期时间。前端后续请求通过 authorization 请求头携带 token。服务端拦截器根据 token 查询 Redis，如果存在就把用户信息放到 ThreadLocal，方便业务层获取当前用户，同时刷新 token 过期时间。

### 2. 签到 Bitmap

源码：

```text
hm-dianping/src/main/java/com/hmdp/service/impl/UserServiceImpl.java
```

核心方法：

```text
sign()
signCount()
```

Redis 结构：

```text
key：sign:{userId}:{yyyyMM}
类型：Bitmap
offset：日期 - 1
value：1 表示已签到，0 表示未签到
```

连续签到统计：

```text
1. 取出本月 1 号到今天的 bitmap
2. 得到一个整数 num
3. 从低位开始判断
4. 连续为 1 就 count++
5. 遇到 0 停止
```

面试回答：

> 签到使用 Redis Bitmap，一个用户一个月一个 key，每一天对应一个 bit 位。签到时把当天对应 bit 置为 1。统计连续签到时，用 BITFIELD 取出本月截至今天的位图，从低位开始连续判断 1 的个数。

---

## 八、推荐学习顺序

### 第 1 天：跑通项目与整体结构

目标：知道项目怎么启动、各模块在哪。

学习内容：

```text
1. 导入 hmdp.sql
2. 启动 MySQL
3. 启动 Redis，注意端口是 63791
4. 启动 Spring Boot 后端
5. 启动 Nginx 前端
6. 访问首页、登录、商户详情
```

必须看懂：

```text
HmDianPingApplication.java
application.yaml
nginx-1.18.0/conf/nginx.conf
```

### 第 2 天：登录鉴权 + 商户缓存

目标：能讲清楚 token 登录、Redis 缓存、缓存穿透、缓存击穿。

重点源码：

```text
UserServiceImpl.java
RefreshTokenInterceptor.java
LoginInterceptor.java
MvcConfig.java
ShopServiceImpl.java
CacheClient.java
```

要能手画流程图：

```text
登录流程图
商户查询缓存流程图
逻辑过期缓存重建流程图
```

### 第 3 天：秒杀 Lua + Redis Stream

目标：能完整讲秒杀链路。

重点源码：

```text
VoucherServiceImpl.java
VoucherOrderServiceImpl.java
seckill.lua
RedisIdWorker.java
RedissonConfig.java
```

必须背熟：

```text
Lua 返回值 0/1/2 的含义
Redis 库存 key
Redis 下单用户 Set key
Stream 队列名 stream.orders
消费者组 g1
为什么需要 Pending List
为什么还要 MySQL 层兜底
```

### 第 4 天：Feed 流 + 点赞 + 关注

目标：能讲清楚 ZSet 为什么适合 Feed 和点赞。

重点源码：

```text
BlogServiceImpl.java
FollowServiceImpl.java
ScrollResult.java
```

必须掌握：

```text
Feed 推模式
ZSet 的 member 和 score 分别是什么
滚动分页 max + offset 的作用
点赞为什么用 ZSet
关注为什么用 Set
共同关注为什么能用 Set 交集
```

### 第 5 天：GEO + Bitmap + 项目复盘

目标：补齐 Redis GEO、Bitmap，并准备面试表达。

重点源码：

```text
ShopServiceImpl.queryShopByType()
UserServiceImpl.sign()
UserServiceImpl.signCount()
HmdpApplicationTest.java
```

必须掌握：

```text
GEO key 设计
GEO 查询流程
为什么还要查 MySQL
Bitmap 签到 key 设计
连续签到统计逻辑
```

---

## 九、面试时的项目介绍模板

可以这样介绍项目：

> 这个项目是一个本地生活点评系统，类似简化版大众点评，主要包括商户查询、探店笔记、点赞关注、Feed 流、签到和优惠券秒杀等功能。后端基于 Spring Boot 和 MyBatis-Plus，数据存储使用 MySQL，Redis 主要用于缓存、登录 token、秒杀库存、Feed 流、GEO 附近商户和 Bitmap 签到。项目的重点是围绕高并发场景做优化，比如热点商户缓存使用逻辑过期解决缓存击穿，秒杀场景使用 Lua 脚本做 Redis 原子校验，并通过 Redis Stream 异步下单削峰，Feed 流使用 SortedSet 实现按时间排序和滚动分页。

如果面试官问“你主要负责哪块”，可以说：

> 我重点学习和实现的是 Redis 相关的高并发优化模块，包括商户缓存、秒杀资格校验与异步下单、关注 Feed 流和附近商户查询。商户缓存主要解决数据库压力问题，秒杀模块重点解决超卖和重复下单问题，Feed 流解决关注动态按时间推送和分页问题，GEO 用于实现基于地理位置的附近商户检索。

---

## 十、你的简历表述优化建议

当前简历写得整体可以，但有一个点建议稍微改准确：

原句：

```text
使用 Lua 脚本保证秒杀资格校验、库存扣减等操作的原子性，避免超卖与重复下单
```

建议改成：

```text
使用 Lua 脚本在 Redis 中原子完成秒杀资格校验、库存预扣减和重复下单判断，并结合 Redis Stream 异步下单，降低数据库瞬时压力
```

原因：

```text
项目里的 Lua 扣减的是 Redis 库存，不是直接扣减 MySQL 库存。
MySQL 库存扣减是在异步消费订单时完成的，并通过 stock > 0 条件更新兜底。
```

更完整的项目亮点可以写成：

```text
- 使用 Redis 缓存热点商户数据，通过缓存空值解决缓存穿透，并使用逻辑过期 + 互斥锁异步重建缓存缓解热点 Key 缓存击穿。
- 使用 Lua 脚本在 Redis 中原子完成秒杀库存预扣减和一人一单校验，并通过 Redis Stream 异步下单削峰，降低数据库瞬时写入压力。
- 使用 Redis SortedSet 实现关注用户的 Feed 收件箱，基于时间戳 score 实现按时间倒序展示和滚动分页加载。
- 使用 Redis GEO 存储商户经纬度，实现按商户类型和用户坐标查询附近商户，并返回距离信息。
```

---

## 十一、必须能回答的 20 个问题

### 商户缓存

1. 为什么要用 Redis 缓存商户详情？
2. 查询商户详情的完整流程是什么？
3. 什么是缓存穿透？项目怎么解决？
4. 什么是缓存击穿？项目怎么解决？
5. 逻辑过期为什么适合热点商户？
6. 商户更新后为什么删除缓存，而不是更新缓存？

### 秒杀

7. 秒杀请求为什么不能直接访问数据库？
8. Lua 脚本里做了哪些操作？
9. Lua 为什么能保证原子性？
10. 如何防止超卖？
11. 如何防止重复下单？
12. Redis Stream 在项目中解决什么问题？
13. Pending List 是什么？为什么要处理？
14. 为什么用了 Lua 之后还需要 Redisson 锁？
15. 为什么用了 Redis 校验之后还要 MySQL 再校验？

### Feed 流

16. Feed 流为什么用 SortedSet？
17. Feed 流是推模式还是拉模式？各有什么优缺点？
18. 滚动分页的 max 和 offset 分别是什么？

### GEO / Bitmap

19. 附近商户查询怎么实现？为什么 GEO 后还要查 MySQL？
20. 签到为什么用 Bitmap？连续签到怎么统计？

---

## 十二、容易被问穿帮的点

### 1. 不要说 Lua 直接扣减了数据库库存

准确说法：

```text
Lua 扣减 Redis 库存，MySQL 库存由后台异步消费订单时扣减。
```

### 2. 不要说项目完全不会重复下单

更严谨说法：

```text
项目通过 Redis Set、Redisson 分布式锁、MySQL 查询做多层防护。生产中建议再加 user_id + voucher_id 唯一索引兜底。
```

### 3. 不要说逻辑过期保证强一致

准确说法：

```text
逻辑过期牺牲短时间一致性，换取热点数据的高可用和低延迟。
```

### 4. 不要说 Feed 流方案适合所有用户

准确说法：

```text
当前推模式适合普通用户。如果是大 V，粉丝量巨大，推送成本会很高，需要使用拉模式或推拉结合。
```

### 5. 不要说 GEO 存了完整商户信息

准确说法：

```text
GEO 只存经纬度和 shopId，完整商户信息仍然从 MySQL 查询。
```

---

## 十三、最终复习检查清单

面试前确认自己能做到：

```text
[ ] 能 1 分钟介绍项目背景和技术栈
[ ] 能画出登录 token 流程
[ ] 能画出商户缓存查询流程
[ ] 能解释缓存穿透、击穿、雪崩的区别
[ ] 能讲清楚逻辑过期的优缺点
[ ] 能逐行解释 seckill.lua 的核心逻辑
[ ] 能讲清楚 Redis Stream 下单流程
[ ] 能讲清楚 Redisson 分布式锁为什么按 userId 加锁
[ ] 能讲清楚一人一单的多层防护
[ ] 能讲清楚 Feed 流的推模式和滚动分页
[ ] 能讲清楚点赞 ZSet 和关注 Set 的设计
[ ] 能讲清楚 GEO 附近商户查询流程
[ ] 能讲清楚 Bitmap 连续签到统计
[ ] 能指出项目还有哪些可优化点
```

项目可优化点可以准备这些：

```text
1. 秒杀一人一单可以增加数据库唯一索引兜底。
2. Redis Stream 消费者可以改成多消费者，提高订单处理能力。
3. 热点商户缓存可以增加缓存预热和随机过期时间，降低冷启动压力和缓存雪崩风险。
4. 图片上传路径目前硬编码，可以改成配置项。
5. Feed 流对大 V 用户可以改成推拉结合，避免写扩散。
6. 登录 token 可以考虑更规范的安全策略，比如更严格的过期、刷新和退出登录处理。
```
