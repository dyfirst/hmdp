services:
  redis:
    image: redis:latest
    container_name: hmdp-redis
    ports:
      - "63791:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes

volumes:
  redis-data:
